# -*- coding: utf-8 -*-\n# @Time    : 2024/1/17 \\:52:28\n# @Author  : sudoskys\n# @File    : test_detect.py\n# @Software: PyCharm\n#\n# This file contains test cases for the fast_langdetect library to demonstrate its capabilities in detecting multiple languages in a single text.\n#\n# Example usage:\n# - Detecting multiple languages in a single string.\n# - Detecting the primary language of a string.\n# - Detecting the language of a string and returning the language code in uppercase.\n#\n# The tests below verify the accuracy of the language detection functions.\n#\n# Multilingual detection example:\n# The function `detect_multilingual` is used to identify multiple languages present in a single text.\n# It returns a list of dictionaries, each containing the detected language and its confidence score.\n#\n# Single language detection example:\n# The function `detect` is used to identify the primary language of a given text.\n# It returns a dictionary with the detected language and its confidence score.\n#\n# Language code detection example:\n# The function `detect_language` is used to identify the primary language of a given text.\n# It returns the language code in uppercase.\n#\n\ndef test_muti_detect():\n    from fast_langdetect.ft_detect import detect_multilingual\n    result = detect_multilingual("Hello, world!你好世界!Привет, мир!", low_memory=False)\n    # Expected output: [{'lang': 'ja', 'score': ...}, {'lang': 'uk', 'score': ...}, {'lang': 'zh', 'score': ...}, ...]\n    assert any(item.get("lang") == "ja" for item in result), "Japanese not detected"\n    assert any(item.get("lang") == "uk" for item in result), "Ukrainian not detected"\n    assert any(item.get("lang") == "zh" for item in result), "Chinese not detected"\n    print("Multilingual detection passed")\n\ndef test_detect():\n    from fast_langdetect import detect    \\n    assert detect("hello world")["lang"] == "en", "English detection failed"\n    assert detect("你好世界")["lang"] == "zh", "Chinese detection failed"\n    assert detect("こんにちは世界")["lang"] == "ja", "Japanese detection failed"\n    assert detect("안녕하세요 세계")["lang"] == "ko", "Korean detection failed"\